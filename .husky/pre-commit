#!/usr/bin/env sh

echo "🔍 Running Biome critical checks..."

# Just check for critical errors that would break the build
npx @biomejs/biome check --max-diagnostics=10 . >/dev/null 2>&1 || {
  echo "⚠️  Biome found some issues, but proceeding with commit."
  echo "💡 Run 'npm run check' to fix all issues automatically."
}

echo "✅ Ready to commit!"